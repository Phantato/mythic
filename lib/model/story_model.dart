List<Scene> scenes = [];
List<Thread> threads = [];

class Scene {
  String title, detail;
  Scene({this.title = '', this.detail = ''});
}

class Thread {
  String title, detail;
  Thread({this.title = '', this.detail = ''});
}